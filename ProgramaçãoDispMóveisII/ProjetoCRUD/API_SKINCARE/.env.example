# PORT=3001
# NODE_ENV=development

# MongoDB Local
# MONGODB_URI=mongodb://localhost:27017/skincare_db

# MongoDB Atlas (descomente e configure)
# MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/skincare_db?retryWrites=true&w=majority

# JWT
# Gere uma chave forte: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# JWT_SECRET=sua_chave_secreta_super_segura_aqui_mude_em_producao
# JWT_EXPIRY=7d

# CORS (separados por vírgula)
# CORS_ORIGIN=http://localhost:3000,http://192.168.1.100:3000
